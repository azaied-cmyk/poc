export default class Delay {
    static defaultRetryCount: number;
    static defaultTimeout: number;
    static setDefaultRetryCount(retryCount: number): void;
    /**
     * addRetryDelay - Add delay before next retry
     * @returns Promise
     */
    static addRetryDelay(): Promise<unknown>;
    static setDefaultTimeout(timeout: any): void;
}
