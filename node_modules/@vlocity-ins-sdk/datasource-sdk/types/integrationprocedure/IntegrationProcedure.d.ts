import DataSourceBrowserDB from "../../common/DataSourceBrowserDB";
import IDataSource from "../DataSource.interface";
import { IntegrationProcedureInput } from "./";
export declare class IntegrationProcedure implements IDataSource {
    private connection;
    private input;
    private apexRestInput;
    private apexRemoteInput;
    private dataSourceDB;
    private actionMode;
    private inPlatformData?;
    constructor(connection: any, input: IntegrationProcedureInput, dataSourceDB: DataSourceBrowserDB, inPlatformData?: Promise<Object>);
    /**
     * Executes the Integration Procedure
     *
     * @returns promise
     */
    execute(): Promise<any>;
    private getIntegrationProcedurePath;
    private ipResponse;
}
