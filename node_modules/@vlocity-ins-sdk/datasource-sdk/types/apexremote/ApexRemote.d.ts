import DataSourceBrowserDB from "../../common/DataSourceBrowserDB";
import IDataSource from "../DataSource.interface";
import { ApexRemoteInput } from "./ApexRemoteInput";
export declare class ApexRemoteDataSource implements IDataSource {
    private connection;
    private input;
    private dataSourceDB;
    private inPlatformData;
    constructor(connection: any, input: ApexRemoteInput, dataSourceDB: DataSourceBrowserDB, inPlatformData?: Promise<Object>);
    /** Executes ApexRemote
     * @returns Promise
     */
    execute(): Promise<any>;
    private visualForceRemoteInvoke;
}
