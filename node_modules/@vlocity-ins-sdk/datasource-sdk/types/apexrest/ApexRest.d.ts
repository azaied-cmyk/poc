import "jsforce/lib/api/apex";
import DataSourceBrowserDB from "../../common/DataSourceBrowserDB";
import IDataSource from "../DataSource.interface";
import { ApexRestInput } from "./";
export declare class ApexRestDataSource implements IDataSource {
    private connection;
    private input;
    private dataSourceDB;
    private inPlatformData;
    constructor(connection: any, input: ApexRestInput, dataSourceDB: DataSourceBrowserDB, inPlatformData?: Promise<Object>);
    /**
     * Executes the ApexRestDataSource
     * @returns Promise
     */
    execute(): Promise<any>;
    /**
     * get ApexRest
     * @param  {ApexRestInput} input
     * @returns Promise
     */
    private jsforceApexRest;
}
