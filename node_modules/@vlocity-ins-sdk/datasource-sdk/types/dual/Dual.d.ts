import DataSourceBrowserDB from "../../common/DataSourceBrowserDB";
import { ApexRemoteInput } from "../apexremote";
import { ApexRestInput } from "../apexrest";
import IDataSource from "../DataSource.interface";
export declare class DualDataSource implements IDataSource {
    useRemote: boolean;
    private connection;
    private apexRestInput;
    private apexRemoteInput;
    private dataSourceDB;
    private inPlatformData?;
    constructor(connection: any, apexRestInput: ApexRestInput, apexRemoteInput: ApexRemoteInput, dataSourceDB: DataSourceBrowserDB, inPlatformData?: Promise<Object>);
    /**
     * Executes the DualDataSource
     * @returns Promise
     */
    execute(): Promise<any>;
}
