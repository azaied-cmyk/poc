import DataSourceBrowserDB from "../../common/DataSourceBrowserDB";
import IDataSource from "../DataSource.interface";
import { SoqlInput } from "./";
export declare class SoqlDataSource implements IDataSource {
    private connection;
    private input;
    private dataSourceDB;
    private inPlatformData?;
    constructor(connection: any, input: SoqlInput, dataSourceDB: DataSourceBrowserDB, inPlatformData?: Promise<Object>);
    /**
     * Executes the SoqlDataSource
     * @returns Promise
     */
    execute(): Promise<any>;
    /**
     * get soql
     * @param  {SoqlInput} input
     * @returns Promise
     */
    private jsforceSoql;
}
