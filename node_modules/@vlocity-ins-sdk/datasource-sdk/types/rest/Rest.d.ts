import "whatwg-fetch";
import DataSourceBrowserDB from "../../common/DataSourceBrowserDB";
import IDataSource from "../DataSource.interface";
import { RestInput } from "./RestInput";
export declare class RestDataSource implements IDataSource {
    private input;
    private dataSourceDB;
    private inPlatformData?;
    constructor(input: RestInput, dataSourceDB: DataSourceBrowserDB, inPlatformData?: Promise<Object>);
    /**
     * Executes the Rest
     * @returns Promise
     */
    execute(): Promise<any>;
    private Rest;
}
